# LoRA 提示词编码器

LoRAPromptEncoder 节点的前端交互领域：LoRA 多选、触发词管理与提示词编辑的一体化上下文。

## 语言

**触发词**:
绑定到某个 LoRA 的固定提示词文本。选中或启用该 LoRA 时追加到正面提示词末尾，移除或禁用时从提示词中删除。
_避免_: 激活标签、提示片段

**已选 LoRA**:
`selectedLoras` 中的条目，包含 `name`、`strength`、`enabled` 三个字段，随工作流持久化到隐藏 widget。
_避免_: 卡片、选中模型

**禁用 / 启用**:
已选 LoRA 的临时状态切换。禁用 = 跳过模型加载 + 从提示词移除触发词，但条目保留、强度不变，可随时恢复；启用 = 恢复加载 + 触发词重新追加到正面提示词末尾。
_避免_: 暂时删除、隐藏

**移除（关闭）**:
永久删除已选 LoRA 条目，同时移除其触发词。入口是已选列表的 ✕ 按钮和网格中点击启用状态的卡片。
_避免_: 禁用

## 标记的歧义

- **"禁用"不是"删除"**：禁用保留条目、仅暂停生效；移除是永久删除条目。UI 对应关系：已选列表的 toggle 开关 = 禁用/启用，✕ 按钮 = 移除。
- **网格卡片点击语义依赖当前状态**：点击启用中的卡片 = 移除；点击已禁用的卡片 = 重新启用（恢复触发词且保留强度）。已禁用卡片以 0.45 透明度弱化显示作为视觉线索。

## 示例对话

> 开发：用户点了 ✕ 之后触发词去哪了？
> 领域专家：触发词从正面提示词里删掉了，条目也删掉了——这是"移除"。
>
> 开发：那用户只是想暂时不用这个 LoRA 呢？
> 领域专家：那是"禁用"：拨一下开关，触发词临时撤走、模型不加载，但条目和强度都留着。再拨回来，触发词追加回提示词末尾，一切照旧。
